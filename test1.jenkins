#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        label 'master'
        }
    triggers { pollSCM('* * * * *') }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
        }
    stages {
        stage("Git clone") {
            steps {
                sh 'git clone https://github.com/Kw1nT/nginx_conf_update && pwd '
               }
            }
        stage("Start ansible playbook") {
            steps {
                sh 'uptime'
              }
           }
        }
     }
